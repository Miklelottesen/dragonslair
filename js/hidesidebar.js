/*********************************
Hide the sidebar on document ready
This script is used on pages, 
that shouldn't have a sidebar
*********************************/

$(document).ready(function(){
	$( 'aside' ).css( 'display' , 'none' );
});