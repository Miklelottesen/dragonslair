$(document).ready(function(){
	//Hide sidebar on document ready
	//This script is to be used on pages, other than the Home page, that shouldn't have a sidebar
	$( 'aside' ).css( 'display' , 'none' );
});